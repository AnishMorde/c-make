#include <stdio.h>
#include "main_utils.h"

int main(){
    
    int result = calculateDistance(12 ,34);
    printf("Total distance is : %d \n" , result);

    // result = calcNewDist(12 ,34);
    // printf("Total distance is : %d \n" , result);

    return 0;
}